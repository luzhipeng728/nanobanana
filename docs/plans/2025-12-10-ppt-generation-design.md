# PPT 生成节点设计文档

> 创建时间：2025-12-10
> 状态：已确认，待实现

## 1. 概述

为 NanoBanana 画布应用添加 PPT 生成功能，采用三节点流水线架构，利用 Claude pptx Skill 实现智能 PPT 生成。

## 2. 设计决策

| 决策点 | 选择 | 说明 |
|-------|------|------|
| 内容来源 | 混合模式 | 支持画布素材连接 + AI 智能组织 |
| 输出格式 | 在线预览 + 下载 | 先预览效果，满意后导出 .pptx |
| 设计风格 | 模板 + 自定义 | 预设模板作为起点，可调整色调、字体 |
| 节点流程 | 三节点流水线 | PPTGenNode → PPTEditorNode → PPTNode |
| 技术实现 | Claude pptx Skill | 利用已安装的 document-skills 插件 |
| 输入方式 | 连接线模式 | 与现有节点系统一致 |

## 3. 节点架构

```
画布素材                    PPT 生成流水线
┌─────────┐     ┌─────────────┐    ┌─────────────┐    ┌─────────┐
│ImageNode│────▶│ PPTGenNode  │───▶│PPTEditorNode│───▶│ PPTNode │
└─────────┘     │             │    │             │    │         │
┌─────────┐     │ - 主题输入   │    │ - 幻灯片预览 │    │ - 全屏预览│
│ImageNode│────▶│ - 模板选择   │    │ - 拖拽排序   │    │ - 翻页播放│
└─────────┘     │ - 风格配置   │    │ - 编辑内容   │    │ - 下载PPT │
┌─────────┐     │ - 生成按钮   │    │ - 删除/添加页│    └─────────┘
│ (文本)  │────▶└─────────────┘    └─────────────┘
└─────────┘
```

## 4. PPTGenNode 详细设计

### 4.1 UI 布局

```
┌─────────────────────────────────────┐
│  🎨 PPT 生成                    ⚙️  │
├─────────────────────────────────────┤
│  主题/标题                          │
│  ┌─────────────────────────────┐   │
│  │ 输入演示文稿主题...          │   │
│  └─────────────────────────────┘   │
│                                     │
│  补充说明（可选）                    │
│  ┌─────────────────────────────┐   │
│  │ 详细描述内容要求...          │   │
│  └─────────────────────────────┘   │
│                                     │
│  模板风格                           │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐      │
│  │商务│ │科技│ │简约│ │创意│      │
│  └────┘ └────┘ └────┘ └────┘      │
│                                     │
│  主色调  🔵 ──────────○            │
│                                     │
│  已连接素材: N 张图片                │
│                                     │
│  ┌─────────────────────────────┐   │
│  │        ✨ 生成 PPT           │   │
│  └─────────────────────────────┘   │
│                                     │
│  ● 输入                      输出 ○ │
└─────────────────────────────────────┘
```

### 4.2 Props 接口

```typescript
interface PPTGenNodeData {
  topic: string;
  description: string;
  template: 'business' | 'tech' | 'minimal' | 'creative';
  primaryColor: string;
  status: 'idle' | 'generating' | 'completed' | 'error';
  taskId?: string;
  error?: string;
}
```

## 5. PPTEditorNode 详细设计

### 5.1 UI 布局

```
┌──────────────────────────────────────────────────┐
│  ✏️ PPT 编辑器                              ⚙️   │
├──────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────┐ │
│  │                                            │ │
│  │           当前幻灯片预览                    │ │
│  │                                            │ │
│  │    ┌──────────────────────────┐           │ │
│  │    │     幻灯片内容            │           │ │
│  │    └──────────────────────────┘           │ │
│  │                                            │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ◀  2 / 5  ▶                      [编辑此页]    │
│                                                  │
│  缩略图导航（可拖拽排序）                         │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐  [+]     │
│  │ 1  │ │ 2● │ │ 3  │ │ 4  │ │ 5  │           │
│  └────┘ └────┘ └────┘ └────┘ └────┘           │
│                                                  │
│  ┌──────────────┐  ┌──────────────┐            │
│  │   🔄 重新生成  │  │   ✅ 确认完成  │            │
│  └──────────────┘  └──────────────┘            │
│                                                  │
│  ● 输入                                 输出 ○  │
└──────────────────────────────────────────────────┘
```

### 5.2 功能列表

- 幻灯片预览：显示当前选中页
- 翻页导航：左右箭头 + 页码
- 缩略图栏：支持拖拽排序
- 编辑功能：修改标题/内容、删除页面、添加新页
- 重新生成：单独重新生成当前页
- 确认完成：传递数据到下游节点

## 6. PPTNode 详细设计

### 6.1 UI 布局

```
┌──────────────────────────────────────────┐
│  📊 PPT 演示                         ⚙️  │
├──────────────────────────────────────────┤
│  ┌────────────────────────────────────┐ │
│  │                                    │ │
│  │        幻灯片预览区域               │ │
│  │                                    │ │
│  └────────────────────────────────────┘ │
│                                          │
│     ◀  1 / 5  ▶        [⛶ 全屏播放]     │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │          ⬇️ 下载 PPT (.pptx)        │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ● 输入                                  │
└──────────────────────────────────────────┘
```

### 6.2 功能列表

- 幻灯片预览：展示最终效果
- 全屏播放：键盘左右键翻页，ESC 退出
- 下载导出：生成 .pptx 文件下载

## 7. API 路由设计

### 7.1 路由结构

```
src/app/api/ppt/
├── generate/route.ts      # 调用 Claude pptx skill 生成 PPT
├── task/route.ts          # 任务状态轮询
├── update/route.ts        # 保存编辑后的 PPT 数据
└── export/route.ts        # 导出 .pptx 文件下载
```

### 7.2 生成 API

```typescript
// POST /api/ppt/generate
interface GenerateRequest {
  topic: string;
  description?: string;
  template: 'business' | 'tech' | 'minimal' | 'creative';
  primaryColor: string;
  materials: Array<{ type: 'image' | 'text'; url?: string; content?: string }>;
}

interface GenerateResponse {
  taskId: string;
  status: 'pending' | 'processing' | 'completed' | 'error';
}
```

### 7.3 任务轮询 API

```typescript
// GET /api/ppt/task?id=xxx
interface TaskResponse {
  status: 'pending' | 'processing' | 'completed' | 'error';
  progress?: number;
  slides?: Slide[];
  fileId?: string;
  error?: string;
}
```

## 8. 数据模型

### 8.1 Prisma Schema

```prisma
model PPTTask {
  id          String   @id @default(cuid())
  userId      String?
  topic       String
  description String?  @db.Text
  template    String
  primaryColor String
  materials   String   @db.Text  // JSON: 输入素材
  slides      String?  @db.Text  // JSON: 生成的幻灯片数据
  fileId      String?            // Claude Files API 返回的文件 ID
  status      String   @default("pending")
  progress    Int      @default(0)
  error       String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

### 8.2 幻灯片数据结构

```typescript
interface Slide {
  id: string;
  layout: 'title' | 'content' | 'two-column' | 'image-focus' | 'ending';
  title: string;
  subtitle?: string;
  content?: string[];      // 要点列表
  imageUrl?: string;       // 图片 URL
  notes?: string;          // 演讲备注
}

interface PPTData {
  title: string;
  template: string;
  primaryColor: string;
  slides: Slide[];
}
```

## 9. Claude pptx Skill 调用

### 9.1 关键配置

```typescript
const response = await client.beta.messages.create({
  model: "claude-sonnet-4-5-20250929",
  max_tokens: 8192,
  // 启用三个 beta 功能
  betas: [
    "code-execution-2025-08-25",  // 代码执行
    "files-api-2025-04-14",        // 文件 API
    "skills-2025-10-02"            // Skills API
  ],
  // 配置 container 使用 pptx skill
  container: {
    skills: [
      {
        type: "anthropic",
        skill_id: "pptx",
        version: "latest"
      }
    ]
  },
  // 启用 code_execution 工具
  tools: [
    {
      type: "code_execution_20250825",
      name: "code_execution"
    }
  ],
  messages: [
    { role: "user", content: userPrompt }
  ]
});
```

### 9.2 环境变量

```env
ANTHROPIC_API_KEY=sk-ant-xxx...
```

## 10. 文件结构

```
src/
├── components/nodes/
│   ├── PPTGenNode.tsx           # PPT 生成配置节点
│   ├── PPTEditorNode.tsx        # PPT 编辑器节点
│   └── PPTNode.tsx              # PPT 输出/下载节点
├── app/api/ppt/
│   ├── generate/route.ts        # 生成 API
│   ├── task/route.ts            # 任务状态 API
│   ├── update/route.ts          # 更新 API
│   └── export/route.ts          # 导出下载 API
└── app/actions/
    └── ppt-task.ts              # Server Actions
```

## 11. 实现优先级

1. **Phase 1**：PPTGenNode + 生成 API（核心功能）
2. **Phase 2**：PPTNode + 导出 API（基础预览和下载）
3. **Phase 3**：PPTEditorNode（编辑功能）

## 12. 风险与注意事项

- **API 成本**：Claude Skills API 是付费服务，需监控用量
- **生成时间**：PPT 生成可能耗时较长，需要任务轮询机制
- **文件大小**：大型 PPT 文件的传输和存储
- **Beta API**：Skills API 仍为 beta 版本，API 可能变化
